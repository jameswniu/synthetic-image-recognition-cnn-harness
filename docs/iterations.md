# Iteration Log

One row per loop iteration; the gates and bands live in EVALS.md. Detection numbers are F1 at
IoU 0.5 against the corrected page labels (four real samples, 287 scored boxes); cls is
classification accuracy on matched boxes; amb is boxes carrying an ambiguity reason.

| run | what ran | detection F1 (s1/s2/s5/s7) | cls | amb (s1/s2/s5/s7) | what changed and what it taught |
|---|---|---|---|---|---|
| v0 probe | 40-line line-hole prototype, scratchpad | found 29/195/227/59 boxes | n/a | n/a | idea holds; glyph holes and narrow cells flood the candidates; HSV gating killed the watermarked borders and every dark X on one sample; thick X strokes split interiors |
| v1 skeleton | detector + ink rule + API + booth | 1.0/1.0/1.0/1.0 vs seeded labels (trivial by construction) | 1.0 | 1/38/12/0 | labels seeded from the detector score perfect by definition; the value was the harness, the overlays, and the first honest counts (43/119/57/79) |
| v2 corrections | labels corrected by eye; aspect floor 0.78, ring-ink filter, kernel union + residue, line-end extension, thin-mark span rule, stray-stroke trace | 1.000 on all four | 0.997 (the scan-faded X stays ambiguous by design) | 2/38/12/0 | the four planted hard cases each forced a named, testable rule; sample_5's nine narrow cells and sample_1's sidebar hole became regression rows |
| v3 gates | tier-1 tests, synth generator + sweeps, reproducibility digest | 1.000 real; sweeps 0.97 to 0.99 F1 under rotation to 5 deg, JPEG to q22, pen, shading, watermark | 0.997 | unchanged | two gate catches in one run: `hash()` is process-salted so synth was not byte-stable (crc32 fixed it), and the 0.4x scale sweep collapsed to F1 0.54, tracing to border breakage at low width |
| v4 witness B | monotone line alignment replacing uniform scaling; small pages upscaled 2x before processing | 1.000/0.988/1.000/1.000 | 0.997 | 2/47/12/0 | measured first: the best global scale fit leaves per-section offsets of hundreds of pixels across vendors, in both directions, and smoothing the profiles destroyed template discrimination; correspondence alignment fixed it (118/118 and 79/79 agree, trusted), the 0.4x scale collapse fell to the upscale fix, and the row-band dedupe exposed three bogus template extrapolations on overlapping windows |
| v5 freeze | span-ceiling fix (the 0.7 threshold was unreachable: the inset interior itself spans 0.70 of the box diagonal, so no clean X could reach it), row windows tightened to 0.55x side with fully-explained-row extrapolation, gold folded (76 cards, two suspected misclicks flagged and excluded), classifier retrained 12 epochs gold-excluded | 0.988/1.000/1.000/1.000 | 0.997 | 2/2/0/0 | ambiguity on the clean render fell 47 to 2; the one miss and one state error are the labeler-ruled faint box and the routed faded X; the witness gate flagged one box on sample_2 that witness B refuses to place (read at the time as a vendor extra box; v6 shows it was witness A's own false positive); int8 classifier 0.993 held-out |
| v6 phantom | per-page height consensus paired with an interior text test in witness A; sample_2 label corrected; figures and sweeps re-run | 0.988/1.000/1.000/1.000 | 0.997 | 2/0/0/0 | the v5 reading was wrong and the gate that exposed it was already there. Witness A had invented a box around the printed word `Att.`, and because the labels were seeded from witness A the invented box sat in the ground truth too, so it graded itself correct and sample_2 read precision 1.000 while carrying a false positive. Witness B was the only independent signal, though a blunter one than I claimed at the time: it matched its three expected slots to the first three boxes in that row in order, so the invented box absorbed a slot and the REAL box at x=487 took the EXTRA_BOX flag. The count was right, the accusation was wrong, and I explained the whole thing away as a vendor quirk. Measured across all nine pages: real boxes sit within 9% of their page's median height and the phantom was the sole outlier at 54 against a median of 48, but height alone would delete real boxes on a form with two box sizes, so the rejection needs both signals. Scored gold falls 288 to 287 and the flag rate falls 1.4% to 0.7%, because both of sample_2's flags were about the phantom |
| v7 argument | acceptance criteria moved out of classify.py into policy.json; telemetry over the whole 61-page corpus; the operator dashboard; both decks | 0.988/1.000/1.000/1.000 | 0.997 | 2/0/0/0 | no number moved, which was the requirement: the shipped policy reproduces the defaults field by field and a gate asserts it. What changed is who can move them. A second policy file reads two boxes differently and scores 286/286 against 285/286, and it is not simply better: it rescues the scan-faded X by refusing to overrule ink on a fragmented mark, which is the same rule that would let a scribbled-out box report as filled. Telemetry over 5,872 boxes finally makes the queue legible: STRAY_STROKE 64, MISSING_IN_DETECT 56, INK_AMBIGUOUS 16, EXTRA_BOX 9, and the held-out real appraisals flag nothing at all. With the patch classifier on, the real pages flag 56 of 847 boxes (6.6%) against 2 with it off, so it is noisiest exactly where the documents are real |
